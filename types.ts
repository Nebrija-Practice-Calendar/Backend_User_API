export type UserType = {
    id: string;
    username: string;
    email: string;
    password: string;
    role: string;
};

export type RoleType = {
    id: string;
    name: string;
    description: string;
};
